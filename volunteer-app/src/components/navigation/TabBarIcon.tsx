// You can explore the built-in icon families and icons on the web at https://icons.expo.fyi/

import Icon, { IconProps } from "../ui/Icon";

export function TabBarIcon({ ...rest }: IconProps) {
  return <Icon size={28} {...rest} />;
}
